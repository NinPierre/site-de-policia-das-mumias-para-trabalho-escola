services:
  - type: web
    name: site-policia-mumias
    runtime: bun
    plan: free
    branch: main
    buildCommand: bun install && bun run build
    startCommand: bun .output/server/index.mjs
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true
